const producto = 'Monitor 20 pulgadas ';
const precio = '30 USD';

console.log(producto.concat(precio)) //Concatenar strings

console.log(producto + "con un precio de " + precio) //Otra forma de concatenar

console.log(`El producto ${producto} tiene un precio de ${precio}`) //Otra forma, con templates